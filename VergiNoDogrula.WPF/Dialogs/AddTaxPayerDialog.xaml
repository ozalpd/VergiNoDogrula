<Window x:Class="VergiNoDogrula.WPF.Dialogs.AddTaxPayerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Yeni Vergi Mükellefi Ekle" Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- TaxNumber Label -->
        <Label Grid.Row="0" Grid.Column="0" Content="Vergi No:"/>
        <TextBox Grid.Row="0" Grid.Column="1" Name="TaxNumberTextBox" Margin="0,0,0,8" TextChanged="OnTextChanged"/>

        <!-- Title Label -->
        <Label Grid.Row="1" Grid.Column="0" Content="Ünvan:"/>
        <TextBox Grid.Row="1" Grid.Column="1" Name="TitleTextBox" Margin="0,0,0,8" TextChanged="OnTextChanged"/>

        <!-- Error Messages -->
        <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Name="ErrorMessage" 
                   Foreground="Red" Margin="0,0,0,8" TextWrapping="Wrap"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Tamam" Width="80" Click="OkButton_Click" Margin="0,0,8,0" Name="OkButton" IsEnabled="False"/>
            <Button Content="İptal" Width="80" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
